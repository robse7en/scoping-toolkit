# Project Constraints

> Generated by `interviewer` agent. This file is loaded by every downstream agent
> (architect, planner, task-writer, scope-reviewer, implement sessions, qa-reviewer).
> Treat answers here as binding unless explicitly amended.
>
> The sections below are common starting points, not a mandatory checklist — mark
> a section "N/A for this project" rather than leaving it blank if it genuinely
> doesn't apply, and use "Project-Specific Considerations" at the end for anything
> important to this particular project that doesn't fit elsewhere (e.g. real-time
> requirements, third-party integrations, regulatory domain specifics, data
> import/migration needs).

## Project Identity
- **Name:**
- **Type:** new | extension
- **One-line description:**

## Tech Stack
- **Backend:**
- **Frontend:**
- **Styling:**
- **Database:**
- **Cloud/Hosting:**
- **Other notable libraries/frameworks:**

## Users & Access Model
- **Multi-user:** yes | no
- **Multi-tenancy required:** yes | no | n/a (single-user)
- **Model:** (e.g. row-level via TenantId / separate schema per tenant / separate DB per tenant / per-user ownership only, not org-level)
- **Notes:**

## Security
> Mark "N/A — no accounts, no sensitive data" if genuinely not applicable, rather
> than leaving blank.
- **Authentication approach:**
- **Authorization model:** (e.g. role-based, claims-based, policy-based)
- **Sensitive data handled:** (e.g. ID documents, photos, PII)
- **Compliance requirements:** (e.g. GDPR, none specified)

## Existing System Context
> Populated only when type = extension. See `existing-system-context.md` for full detail.
- **Existing stack confirmed compatible:** yes | no | n/a
- **Existing auth system to integrate with:**
- **Existing conventions to follow:**
- **Integration points confirmed for reuse:** (which ones from existing-system-context.md the user confirmed should actually be used)

## Existing System — Resolved Inconsistencies
> Populated only when type = extension AND codebase-analyst flagged inconsistencies.
> Each item codebase-analyst found under "Inconsistencies Found" gets an explicit
> user decision here — architect-agent acts on this, not on the analyst's raw
> observation.
-

## Non-Functional Constraints
> Mark "N/A" for any that genuinely don't apply rather than leaving blank.
- **Performance:**
- **Offline/connectivity requirements:**
- **Scale expectations:**
- **Browser/device support:**

## Project-Specific Considerations
> Anything important to this project that the standard sections above don't
> capture — e.g. real-time/latency requirements, third-party integrations,
> data import/export or migration needs, regulatory or domain-specific
> requirements, hardware/device constraints. Leave empty if nothing applies.
-

## Explicit Out-of-Scope
> Things the user explicitly said NOT to build, to prevent feature-suggester or
> task-writer from reintroducing them later.
-

## Open Questions
> Anything unresolved that should block planner until answered.
-
