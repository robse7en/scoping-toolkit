# Scope Convergence Report

> Generated by `scope-verifier` in convergence mode. Read-only report.
> This file compares implemented code with the scoped intent. It does not
> reopen tasks or add new ones automatically.

**Generated:** (timestamp)
**Target:** all | <task-id>

## Summary

- **Missing:**
- **Partial:**
- **Contradicts scope:**
- **Unrequested work:**

## Findings

| Type | Location | Summary | Recommended follow-up |
|---|---|---|---|

## Notes

- Use `missing` when required scoped work is absent.
- Use `partial` when work exists but does not fully satisfy acceptance criteria.
- Use `contradicts` when code conflicts with constraints, architecture, or principles.
- Use `unrequested` when code introduces behavior outside approved scope.
